.fb-logout-block
  - unless current_user
    = link_to "Sign in with Facebook", user_facebook_omniauth_authorize_path, class: 'fb'
  - else
    .fb
      = current_user.name
      = link_to "Logout", destroy_user_session_path, method: :delete, class: 'fb-logout'
